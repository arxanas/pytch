name: pytch
file_extensions: ["pytch"]

scopes:
  comment:
    vscode: "comment"
    pygments: "Comment"

  whitespace:
    vscode: "TODO"
    pygments: "Whitespace"

  keyword:
    vscode: "keyword"
    pygments: "Keyword"

  identifier:
    vscode: "entity.name"
    pygments: "Name"

  numeric:
    vscode: "constant.numeric"
    pygments: "Number"

  punctuation:
    vscode: "punctuation"
    pygments: "Punctuation"

  text:
    vscode: "text"
    pygments: "String"

contexts:
  root:
    - scope: whitespace
      regex: \s+

    - scope: comment
      regex: \#[^\n]*

    - scope: keyword
      regex: |-
        and
        | else
        | if
        | let
        | or
        | then

    - scope: identifier
      regex: "[a-zA-Z_][a-zA-Z0-9_]*"

    - scope: numeric
      regex: "[0-9]+"

    - scope: punctuation
      regex: |-
        =  |
        ,  |
        \+ |
        \- |
        \( |
        \)

    - scope: text
      regex: .
      begin:
        regex: \"
        scope: text
      end:
        regex: \"
        scope: text
